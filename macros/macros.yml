macros:
  - name: rolling_average_generalised
    description: 'This is a macro that takes 4 arguments (column_name, partition_by, order_by, rolling_average_period) and \n
                  aggregates the column_name over partition_by and order_by fields over rolling_average_period.'
  - name: get_monthly_series
    description: 'This is a macro that creates a monthly date series from int_dates. You can select day of month, where default is 1.'